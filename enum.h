#ifndef ENUM_H
#define ENUM_H

enum enum_tool
{
    plink = 0,
    gemmax = 1,
    emmax = 2,
};

enum enum_model
{
    glm = 0,
    mlm = 1,
    linear_regression = 2,
};

enum enum_user_OS
{
    windows = 0,
    linux = 1,
};


#endif // ENUM_H
